# self-service

This is a Java Spring Boot microservice project.

- Project name: self-service
- Base package: mx.bastekor.selfservice

## Getting Started

To build and run the project:

```
./mvnw spring-boot:run
```

Or on Windows:
```
mvnw.cmd spring-boot:run
```

## Project Structure
- The main application class is in `src/main/java/mx/bastekor/selfservice/SelfServiceApplication.java`.
- Standard Spring Boot structure is used.

---

This project was generated using [Spring Initializr](https://start.spring.io/).
